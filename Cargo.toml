[package]
name = "stormview"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "The storm view contract — component summaries, typed relations, metrics and actions shared by every storm UI (stormd, stormsh, stormdrive, stormconsole)"
repository = "https://github.com/glennswest/stormview"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
